<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.NiceStart" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NiceStart.NoActionBar" parent="Base.Theme.NiceStart">
        <!-- PARA QUE NO APAREZCA PRIMERO EL CARGAR DE ANDROID Y LUEGO EL SPLASH -->
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="Theme.NiceStart.ActionBar" parent="Theme.Material3.DayNight">
        <!-- <item name="android:textColorPrimary">@android:color/white</item> -->
        <!-- Estilo del ActionBar -->
        <!--        <item name="actionBarStyle">@style/TransparentActionBar</item>-->
    </style>

    <style name="TransparentActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@android:color/transparent</item>
        <item name="android:elevation">0dp</item> <!-- Quitar sombra si es necesario -->
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.NiceStart.ActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>